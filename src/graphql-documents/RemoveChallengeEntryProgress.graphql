mutation RemoveChallengeEntryProgress(
  $challengeEntryProgressId: ID!
) {
  deleteChallengeEntryProgress( input: { id: $challengeEntryProgressId }) {
    challengeEntryProgress { 
      id
    }
  } 
}
