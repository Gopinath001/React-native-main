mutation CreateAddress($input: createUserAddressInput!) {
  createUserAddress(input:$input) {
    userAddress {
      id
      company
      address1
      address2
      city
      province
      zip
      country
      phone
    }
  }
}
