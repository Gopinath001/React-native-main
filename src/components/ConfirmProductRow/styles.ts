import { css } from "@emotion/react"
import { colorBrandPrimary, sizeFontLarge } from "@rap-theme"

export const priceStyles = css({
	color: colorBrandPrimary,
	fontWeight: 300,
	fontSize: sizeFontLarge,
})

export const blueTextStyles = css({
	color: colorBrandPrimary,
	fontWeight: 300,
})
